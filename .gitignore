# build output
dist/
.astro/

# deps
node_modules/

# env
.env
.env.production

# os
.DS_Store

# 참고: src/content/blog/ 와 public/images/ 는 sync 로 생성되지만
# 저장소를 자기완결적으로 만들기 위해 '커밋'합니다 (CI가 output/ 없이 빌드).

# 로컬 개발 도구 설정
.claude/
